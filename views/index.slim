ruby:
  total_libs = pods.size.round(-3)
  total_apps = total_stats.order_by(:id.desc).first[:app_total].round(-5)
  libraries = ActiveSupport::NumberHelper.number_to_human(total_libs, locale: "en-GB").downcase
  apps = ActiveSupport::NumberHelper.number_to_human(total_apps, locale: "en-GB").downcase


== partial "search-templates"
== partial "search"

== partial "results"

#content-wrapper
  .homepage_container.container

    section.row

      .col-md-8.col-lg-8.col-sm-8.col-md-offset-2.col-lg-offset-2.col-sm-offset-2.intro
        h3.underlined What is CocoaPods
        <div class="header-horizontal-divider"><div class="index-arrow-down"></div><div class="index-enclosed-arrow-down"></div></div>

        p == "CocoaPods is a dependency manager for Swift and Objective-C Cocoa projects. It has over #{libraries} libraries and is used in over #{apps} apps. CocoaPods can help you scale your projects elegantly."


    section.row
      .col-md-8.col-lg-8.col-sm-8.col-md-offset-2.col-lg-offset-2.col-sm-offset-2

        ul.nav.nav-tabs.underscore

          li.active
            a href="#install" data-toggle="tab" Install

          li
            a href="#get_started" data-toggle="tab" Get Started

          li
            a href="#create_a_pod" data-toggle="tab" Create a Pod

        <div id="homepage-tab-indicator" class="index-horizontal-divider"><div class="index-arrow-down"></div><div class="index-enclosed-arrow-down"></div></div>


        div.tab-content
          div#install.tab-pane.fade.in.active

            markdown:

              CocoaPods is built with Ruby and is installable with the default Ruby available on OS X.
              We recommend you use the default ruby.

              Using the default Ruby install can require you to use `sudo` when installing
              gems. Further installation instructions are in [the guides](https://guides.cocoapods.org/using/getting-started.html#getting-started).

              <pre class="highlight">
              # Xcode 7 + 8
              $ sudo gem install cocoapods --pre

              # Xcode 7
              sudo gem install activesupport -v 4.2.6
              sudo gem install cocoapods
              </pre>

              We also have a [Mac app](/app) that handles everything you. It only gets major releases ATM though.

          div#get_started.tab-pane.fade

            markdown:
              Search for pods (above). Then list the dependencies in a text file named
              `Podfile` in your Xcode project directory:

              <pre class="highlight">
              platform :ios, '8.0'
              use_frameworks!

              target 'MyApp' do
                pod 'AFNetworking', '~> 2.6'
                pod 'ORStackView', '~> 3.0'
                pod 'SwiftyJSON', '~> 2.3'
              end
              </pre>

              <small>Tip: CocoaPods provides a `pod init` command to create a Podfile with
              smart defaults. You should use it.</small>

              Now you can install the dependencies in your project:

              <pre class="highlight">
              $ pod install
              </pre>

              Make sure to always open the Xcode workspace instead of the project file when
              building your project:

              <pre class="highlight">
              $ open App.xcworkspace
              </pre>

              Now you can import your dependencies e.g.:

              <pre class="highlight">
              #import &lt;Reachability/Reachability.h>
              </pre>


          div#create_a_pod.tab-pane.fade

            markdown:
              Sometimes CocoaPods doesn’t yet have a pod for one of your dependencies. Fortunately, creating a pod is pretty easy:
              <pre class="highlight">
              $ pod spec create Peanut
              $ edit Peanut.podspec
              $ pod spec lint Peanut.podspec
              </pre>

              You can find a lot of information on the [process in the guides](https://guides.cocoapods.org). When you're done you can get an account and push your [pod to the CocoaPods Trunk](https://guides.cocoapods.org/making/getting-setup-with-trunk.html).

    section.row

      .col-md-8.col-lg-8.col-sm-8.col-md-offset-2.col-lg-offset-2.col-sm-offset-2
        h3.underlined Contribute
        <div class="header-horizontal-divider"><div class="index-arrow-down"></div><div class="index-enclosed-arrow-down"></div></div>

        markdown:
         We’re developing CocoaPods on GitHub. There’s a guide for [getting started on the CocoaPods tool](https://guides.cocoapods.org/contributing/contribute-to-cocoapods.html). It’s easy and really gratifying to contribute patches! - for a lot of people it's their first foray into Open Source. We have some [easy tickets to look at](https://github.com/CocoaPods/CocoaPods/issues?q=is%3Aopen+is%3Aissue+label%3Ad1%3Aeasy).
